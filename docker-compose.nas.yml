services:
  research-memory-gateway:
    image: ghcr.io/kettly1260/research-memory-gateway:latest
    container_name: research-memory-gateway
    ports:
      - "8787:8787"
    environment:
      RESEARCH_MEMORY_TOKEN: "change-me"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
      - ./exports:/app/exports
    restart: unless-stopped
